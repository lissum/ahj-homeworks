import "./css/style.css";
import "./js/app";

// Initialize the card validator when the DOM is loaded
document.addEventListener("DOMContentLoaded", () => {
  initCardValidator("#card-number", "#card-icons");
});

// TODO: write your code in app.js
