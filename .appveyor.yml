image: Ubuntu2204
stack: node 20

branches:
  only:
    - video

cache: node_modules

install:
  - cd events
  - yarn

build_script:
  - yarn run build

deploy_script:
  - git config --global credential.helper store
  - git config --global user.name AppVeyor
  - git config --global user.email ci@appveyor.com
  - echo "https://$GITHUB_TOKEN:x-oauth-basic@github.com" > "$HOME/.git-credentials"
  - yarn gh-pages -d dist -u "appveyor-bot <ci@appveyor>" -b gh-pages -r https://github.com/lissum/ahj-homeworks.git --dest events